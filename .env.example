# Local development database — matches the docker-compose.yml postgres service.
# Copy this file to `.env` for local runs. The mock SMS provider is the default,
# so no other variables are required to run the system end-to-end.
DATABASE_URL=postgresql://cadence:cadence@localhost:5433/cadence
